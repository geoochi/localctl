# localctl 配置示例：复制为 .env 并修改（.env 不会被提交）
# 监听地址（仅建议本机回环地址）
LOCALCTL_ADDR=127.0.0.1:8003
# 登录密码（明文，启动时自动做 bcrypt 哈希，不会落盘）
LOCALCTL_PASSWORD=change-me
# Session cookie 的 HMAC 密钥，填任意随机长字符串，例如：
#   openssl rand -base64 32
LOCALCTL_SECRET=replace-with-random-secret
